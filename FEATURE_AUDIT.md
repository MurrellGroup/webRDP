# Feature audit — 0.9.2 checkpoint

This ledger separates product completeness from scientific parity. “Resolved”
means the defect is implemented and covered by a local regression or build
check. It does not imply complete numerical equivalence to the desktop RDP5
package.

| Audit item | 0.9.2 state | Evidence / remaining gate |
| --- | --- | --- |
| Center page or Export view cannot scroll | **Resolved systemically** | The center workspace is the desktop vertical scroll owner. Generic panels grow in document flow instead of imposing a viewport cap; only genuinely data-heavy grids, ledgers, trees, and queues have intentional local scrolling. Below 980 px, scrolling returns to `body`. Source and rendered-CSS regressions enforce the ownership model. |
| Recombination-map rows escape their box and cover later panels | **Resolved structurally** | Every panel is now an isolated clipping boundary without a height cap. The map has a definite 240–420 px scroll viewport with layout/paint containment; expanded mode hands its full content to the panel-body scroll owner. Regression tests require all three containment layers. |
| A panel is too short to inspect comfortably | **Resolved** | Every reusable titled `Panel` has an accessible full-screen control, backdrop dismissal, Escape handling, focus restoration, body scroll locking, and its own expanded scroll body. |
| Alignment viewer is too primitive | **Resolved for multi-parent review** | Base mode and parent-affinity mode; any 2–6 sequences can be selected as parents; unique-parent, multi-parent/shared, none/novel, and missing/ambiguous states are visually distinct. Parent-informative-only columns, three zoom densities, linked event-parent defaults, sticky labels, coordinate tooltips, large-alignment memory guard, and unit tests are included. |
| Complex/nested event workflow is unclear | **Resolved at workflow/UI and component-reconstruction level** | The source-guided Review studio makes the manual's event loop the primary surface: signal → breakpoints → roles → trees → grouping → decision → dependency-aware rescan. Accepted co-recombinant tracts are erased, copied into gap-padded internal components, recursively targeted by lineage, and rescanned. Crossing signals split into continuous pieces with explicit structural breakpoint uncertainty. Historical-event `FindSets` closure now also feeds the source parsimony cascade. Automated ancestral direction remains parity work. |
| Recombinant/parent assignments are difficult to challenge | **Substantially resolved with the default RDP5 source consensus** | The inspector tests all three polarities and persists 18 standalone source statistics, the final-trim penalty, and six joint rules covering profile/tree movement, TrpScore/OuCheck, O:E/O:EDist, SSDist/OUIndex, ParsimonyO/I, Conflict, SetDistT/P, and packed-WASM dMax with direct `MakeConsensusC` weights, VB6 quantization, and the desktop 60% ambiguity threshold. Auto-resolve holds ambiguous/challenged roles. Optional logistic/neural selectors and desktop-corpus calibration remain. |
| Global queue must still be resolved manually one event at a time | **Resolved as an explainable heuristic; calibration remains alpha** | Conservative/balanced/aggressive presets, hard evidence gates, weighted scores, dry-run recommendations, analyst-review bands, locked reviewed decisions, transitive dependency holds, and reversible audit entries are operational. The runner stops at its first causal barrier, rescans impacted targets or all unresolved targets according to policy, excludes accepted mosaic sequences from parent search, suppresses only close same-parent duplicates, replans, and obeys a round cap. RDP5 comparison calibration and independent scientific validation remain required before unattended publication-grade use. |
| Trees look disconnected | **Resolved** | Tree geometry is produced by a tested cumulative-distance layout, non-negative branch sanitization, one continuous SVG path, explicit joints, zero-length branch nodes, and finite-coordinate checks. |
| Tree exploration is too weak | **Resolved for exploratory NJ review** | Dedicated Local trees tab; tract-versus-combined-background default matching the manual, optional left/tract/right comparison, nearest-context cohorts, linked leaf marking across every tree, triad role colors, topology summaries, individual/batch Newick export, and explicit unrooted-tree interpretation. ML trees, RF distances, SH/AU tests, and editable clade operations remain open. |
| Method result ownership is unclear | **Resolved** | Seven named method tabs expose support decision, raw and adjusted p, correction scope, method statistic, calibration, limitation, source paper, and a method-specific profile. Every enabled family now contributes an independent interval; intervals are stored, overlaid, listed in the inspector, and exported rather than being confused with the consensus/BURT interval. A method cannot support a candidate unless its own interval is co-located. |
| React minified error #418 | **Resolved with deterministic SSR text** | Ambient locale/time-zone formatting was removed from rendered dates and times; numeric locale is explicit; the initial tutorial timestamp is fixed; two identical SSR requests must be byte-identical; a static regression forbids unqualified locale formatting. |
| Only one small example | **Resolved** | Eight deterministic, truth-annotated synthetic datasets cover a clean triplet, tutorial virus, larger virus family, circular ssDNA family, segmented family, nested/overlapping mosaics, an 80 kb bacterial core-genome HGT panel, and a 520-genome stress panel. Empirical genomes remain intentionally unbundled pending dataset licensing and provenance. |
| Pattern “heatmaps” are sparse or use the wrong axes | **Resolved for the primary RDP4-style views** | Breakpoint pairs are now genomic-position × genomic-position endpoint counts rather than event similarity. A split region matrix shows observed tract separation counts and signed circular-null residuals; a split local-discordance matrix shows RMS p-distance deviation and profile-correlation loss. Canvas rendering, 48/64/96-bin control, exact hover/keyboard readouts, event activation, and sequential/diverging palettes preserve figure-level density. Exact RDP4 SH/RF/permutation numerical parity remains open and is explicitly not claimed. |
| Unsafe export silently includes stale or unreviewed events | **Resolved** | Sequence-changing outputs default to accepted + fresh. “All fresh” and an explicitly unsafe retained override are visible; rejected hypotheses remain preserved in project/CSV provenance. Earlier scientific edits mark the dependent queue stale. |
| Edited-event multiple testing silently becomes one test | **Resolved** | Recalculation retains original scan multiplicity. Holm edits use a labeled conservative family correction until a full rescan can re-rank hypotheses. |
| Breakpoint confidence remains stale after edits | **Resolved** | Scientific edits collapse old bounds to exact analyst coordinates, mark the event and later review-order dependencies stale, and require recalculation/rescan. |
| Alternative parents are discarded | **Resolved** | Alternative sampled parent indexes survive deduplication, project round-trip, inspector display, and reassignment. Explicit unsampled-parent models remain open. |
| Undo/delete destroys provenance | **Resolved** | Per-event history plus append-only project ledger; undo/redo append entries; deletion preserves a serialized tombstone. `.rdpweb` remains schema 0.5 compatible. |
| No autosave or stopped-scan recovery | **Resolved** | IndexedDB restore/start-fresh flow and partial worker candidate recovery. Recovered evidence is visibly stale. |
| Query/reference mode cannot group or test references | **Resolved** | Full mode tests every allowed query against every pair of concrete references; same named-group pairs can be excluded, and references can optionally be tested as recombinants. Editable/inferred groups diversify only the explicitly approximate shortlist. RDP5 naming-rule parity fixtures remain. |
| Scanner substitutes a pair plus alignment consensus for a real triplet | **Resolved structurally and by default** | A new analysis enumerates every unordered set of three concrete sequences, all enabled method kernels receive three explicit alignment indexes, and all target polarities are evaluated. No consensus/rest-of-alignment proxy exists. Approximate parent pruning is separately named, opt-in, and provenance-marked. A five-sequence worker regression requires exactly C(5,3)=10 comparisons. |
| Disabled methods still consume full statistic cost | **Resolved at discovery/statistic-kernel level** | The WebAssembly bitmask bypasses disabled discovery and evidence kernels, including BootScan resampling. Exact 3SEQ DP is budgeted only for retained candidates. |
| Fixed top-500 candidate loss | **Resolved as bounded adaptive retention** | Up to 5,000 consensus candidates, per-recombinant bounds, distinct tract-size retention, independent method intervals, a tunable 1–256 multi-excursion RDP queue with explicit truncation provenance, and all locally significant source SiScan topology runs. Multiple raw signals for GENECONV, BootScan, MaxChi, Chimaera, and 3Seq remain a parity gate. |
| No command-line batch path | **Resolved for same-engine local batch** | The checked-in worker/Wasm can write a restorable `.rdpweb` project from Node. Standalone packaging and analysis manifests remain. |
| Native RDP project compatibility | **Open** | FASTA, CLUSTAL, PHYLIP, NEXUS, GFF3, GenBank FEATURES, BED, and `.rdpweb` are supported. RDP4/RDP5 project conversion is not. |
| All seven methods discover candidates independently | **Resolved architecturally; numerical parity remains method-specific** | The shared CUSUM seed is removed. RDP, GENECONV, BootScan, MaxChi, Chimaera, SiScan, and 3Seq each emit their own discovery interval, with reverse-parent passes for directional methods and co-location gating before confirmation. RDP retains multiple VNP excursions and SiScan retains every significant source topology run; exact source multi-run event queues for the other five families remain. |
| Exact RDP5 numerical parity | **Partially resolved — critical parity gate** | Source-mode multi-signal RDP localization/probability scaling; finite-G GENECONV scoring and KA probability; compressed-variable-site MAXCHI/CHIMAERA windows and source peak multiplier; source SiScan fourth-sequence/15-category/permutation/topology-run workflow; fixed-three-state BURT plus exact circular padding/crop, confidence scanning, `MatchBPtoCI`, and non-reassortment `PolishBP`; §4.1.6 signal disassembly; `GetSupers`; six-cell distance correlation; SDM filtering; seeded JC/NJ bootstrap/collapse evidence; and all-sequence/all-three-orientation co-recombinant screening have routine-level ports and regressions. Reassortment-boundary BURT, GENECONV indel/overlap/permutation modes, multi-taxon BootScan orchestration, exact MAXCHI/CHIMAERA peak growth, exact `Clearcut`/`CheckBSTree` behavior, multi-signal queues for the other five non-RDP families, and a broad desktop golden corpus remain. |
| Validation breadth | **Partially resolved** | Scientific, full concrete-triplet enumeration, circular, large-path, source-PHI graph/moment, diagnostic, exact-probability, method-mask, example, project, scrolling/full-screen, alignment-affinity, tree-layout, reconstruction, auto-resolution, deterministic SSR, static artifact, subpath, and performance gates pass. RDP5 comparison corpus, ROC curves, and Chrome/Firefox/Safari/Edge E2E remain release blockers. |

## Current release judgment

Version 0.9.2 is a substantially more coherent and testable **scientific
alpha**, not a validated drop-in RDP5 replacement. The next highest-value work
remains completion of each method's source locator/calibration edge behavior
and numerical comparison fixtures, followed by richer ancestral-event
propagation and statistically stronger tree/clade workflows. The audited
manual/source behavior crosswalk is in `SOURCE_WORKFLOW_AUDIT.md`.
